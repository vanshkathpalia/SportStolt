// export const storeReferrer = () => {
//   const referrer = document.referrer;
//   if (referrer && !referrer.includes(window.location.hostname)) {
//     sessionStorage.setItem("externalReferrer", referrer);
//   }
// };